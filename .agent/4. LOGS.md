# LOGS.md

## 2026-08-27
### 架构大重构与代码清理
* **变更内容**:
  * 彻底清理根目录 20+ 个一次性探测脚本与无关项目缓存，建立正规工程目录 `core/` 与 `tests/`。
  * 移除 PDF/Word/ZIP 繁琐附件解压和抢占鼠标双击的遗留代码，聚焦**“静默读取 -> 判断/LLM -> 静默回复”**的核心链路。
  * 保留并标准化 OCR 图像识别能力（`core/ocr_engine.py`），提供惰性加载与优雅降级。
  * 重构微信驱动（`core/wechat_driver.py`），严格遵循 100% 后台静默、纯 UIA 内存注入（`ValuePattern`）、不移动鼠标、不抢占焦点的红线。
  * 统一配置中心（`core/config.py`）与 SQLite 消息查重仓库（`core/storage.py`）。
  * 编写并通过全套单元测试（7/7 Passed）。
* **技术事实**:
  * 微信 4.0 (Qt 5.15.14) 在后台被遮挡时，通过广播 `SPI_SETSCREENREADER` 激活 UIA 控件树，穿透深度达到 30+ 层，读写完全无需前台焦点。
  * SQLite 连接在 Windows 环境下必须显式关闭连接句柄以防文件锁定（WinError 32）。
* **当前状态**: 重构交付完成，测试通过，可由 `python main.py` 或 `start_bot.bat` 启动。
