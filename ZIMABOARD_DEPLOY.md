# 微信 RPA 机器人 Zimaboard 24H 挂机部署指南

为了实现完全的**物理硬件隔离**，让主力 Win11 电脑不受任何无障碍窗口、剪贴板干扰，本指南将协助你将机器人完美迁移至装有 **Windows 10 的 Zimaboard** 独立板卡上，进行 24 小时无人值守挂机运行。

---

## 🛠️ 部署前置准备 (Zimaboard 端)

1. **安装环境**：
   * 在 Zimaboard 上安装 [Python 3.10+](https://www.python.org/downloads/)（安装时勾选 **Add Python to PATH**）。
   * 安装 **微信 4.0 (Windows 重构版)**，并登录你的**机器人小号**。
2. **首次激活无障碍（极度重要）**：
   * 微信 4.0 仅在启动瞬间检测系统读屏标志。在 Zimaboard 上登录小号前，**请先在 Win10 系统里开启一次“讲述人 (Narrator)”**。
   * 彻底退出并重新登录小号微信。
   * 登录成功后，你可以**关闭讲述人**。此后，小号微信的无障碍树接口将被一直保持公开唤醒状态。

---

## 🚚 步骤一：局域网一键同步代码

1. **Zimaboard 开启文件夹共享**：
   * 在 Zimaboard 的盘符下新建文件夹 `D:\wxbot`。
   * 右键该文件夹 -> **属性** -> **共享** -> **共享(S)...** -> 添加 `Everyone` 权限并设置为“读取/写入”，点击共享。
2. **主力机映射与同步**：
   * 在主力 Win11 机上，可以直接通过资源管理器访问 `\\<Zimaboard_IP>\wxbot`。
   * **每次更新代码时**，只需将主力机上的项目文件（除 `.venv` 外）复制覆盖到该共享网络路径下。

---

## ⚙️ 步骤二：Zimaboard 运行依赖安装

在 Zimaboard 上，使用管理员权限打开 CMD / PowerShell 窗口，进入共享的 `D:\wxbot` 目录，执行以下命令建立本地虚拟环境并安装依赖：

```powershell
# 1. 进入工作目录
cd D:\wxbot

# 2. 建立 Python 虚拟环境
python -m venv .venv

# 3. 安装依赖包 (Pillow, pywin32, psutil, uiautomation 等)
.venv\Scripts\pip.exe install -r requirements.txt Pillow pywin32 psutil uiautomation requests
```

---

## 🛡️ 步骤三：配置 24 小时开机自动保活守护

为了保证 Zimaboard 在因系统更新、断电、系统崩溃导致重启后，机器人能 100% 满血自动复原，我们需要配置 **Windows 任务计划程序 (Task Scheduler)**。

### 1. 微信自愈设置
* 检查你的小号微信在设置中开启了 **“开机时自动启动微信”** 以及 **“自动登录”**。

### 2. 挂机脚本自启动设置
1. 在 Zimaboard 上，按 `Win + R` 键，输入 `taskschd.msc` 打开**任务计划程序**。
2. 在右侧点击 **“创建任务...”**：
   * **常规**：
     * 名称输入：`WeChat_Bot_Keeper`。
     * 勾选 **“不管用户是否登录都要运行”** (使脚本完全静默在后台，无黑窗口干扰)。
     * 勾选 **“使用最高权限运行”** (防止 UIPI 限制)。
   * **触发器**：
     * 点击“新建”，开始任务选择 **“启动时”**。
     * 高级设置中勾选 **“延迟任务时间：30 秒”**（给微信开机自启、登录、初始化留足时间）。
   * **操作**：
     * 点击“新建”，操作选择 **“启动程序”**。
     * 程序或脚本输入：`D:\wxbot\start_bot.bat`。
     * 起始于输入：`D:\wxbot\`。
   * **条件**：
     * 取消勾选“只有在计算机使用交流电源时才启动此任务”。
   * **设置**：
     * 取消勾选“如果任务运行时间超过其值，则停止任务”。
     * 勾选 “如果此任务已经运行，以下规则适用：**请勿启动新实例**”。
3. 点击确定，输入 Zimaboard 的 Windows 账户密码保存。

---

## 🧪 步骤四：高可用自愈能力验证

Zimaboard 部署完毕并启动 `start_bot.bat` 守护进程后，系统将处于以下全自动自愈状态：

1. **Python 脚本崩溃自愈**：
   * 如果 Python 因为 API 超时、网络抖动等原因异常退出，`start_bot.bat` 会在 5 秒钟后自动重新运行 `bot.py`。
2. **微信客户端闪退自愈**：
   * 如果小号微信崩溃闪退（HWND 消失），`bot.py` 内部守护机制会在 1 秒内检测到，**自动在注册表中查找微信安装路径并重新拉起微信客户端**，随后每 5 秒自动尝试重新绑定大丑群，完成无缝自愈！
